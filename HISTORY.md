# AF-MilitaryShogi 変更履歴

日時は日本標準時（`YYYY-MM-DD HH:MM:SS +09:00`）で記録する。新しい版を上に追加する。

## 1.2.1 — 2026-09-23 21:40:14 +09:00（受入試験中・未確定）

- 1.2.0の人間受入試験で判明した「効果音が一切出ない」不具合を修正。原因：カメラを実行時に `AddComponent<Camera>()` で生成しており、AudioListenerが1つも存在しなかったため、Unityが何もミックス出力していなかった（直接の `PlayOneShot` でも出力ピーク0）。メインカメラにAudioListenerを追加。
- 効果音クリップを起動時にデコード（初回の再生が遅れない・落ちないように）。
- 実出力の診断 `-audioprobe FILE` を追加：AudioListener・AudioSource・AudioClipの実行時状態と、`AudioListener.GetOutputData` による出力ミックスのピークを、直接再生・駒クリック経由・効果音OFF・音量100%/50%で測定。1.2.0のタイミング検証は再生要求の時刻しか見ておらず、可聴出力を保証していなかった。EXE自動検証にもAudioListenerの存在確認を追加。
- 検証は軽量のみ（ビルド、修正前後の `-audioprobe`）。フル回帰試験は未実施で、リリース候補の段階でまとめて実施する。最終判定は人の実聴。
## 1.2.0 — 2026-09-23 21:00:02 +09:00

- 1.1.1を基準点とした機能追加。CPU（強さ・2手読み・評価・重み・配置思想・Knowledge・Decision Seed・情報隠蔽）とルールは変更なし。決定的CPU評価指標は1.1.1と全行一致、実EXEの指紋も1.1.1と同一（72C15D3F2B9D83FF）。
- 配置プリセット5枠（両モード）：初期名「プリセット1〜5」、保存時は枠の現在名を初期値にした名前欄のみ。各マスのPieceTypeを形式バージョン付きで保存し、保存・読込時に検証して不正データを拒否。CPUのSeed・配置・Knowledgeは不変。再起動後も保持。
- 研究モード初期配置の重複ボタン「おまかせ配置（Player Seedで再配置）」を削除（Seedの適用／乱数に一本化）。
- 1クリックのモード切替（「研究モードへ」／「対戦モードへ」、F2）。設定画面からモード切替を削除。
- 設定画面：戦闘演出（通常／簡易）・速度（×1／×2／×4）・敵駒の観測情報（ON／OFF）・効果音（ON／OFF）・音量。即時反映・保存。
- 研究モード「CPU駒の正体を表示」ONで盤上のCPU駒の表面・ツールチップにも真値を表示（OFFで判明／正体不明へ戻る）。対戦モードでは常に無効、損失表示は裏面のまま。
- 経過時間（ヘッダー・結果画面）：対局開始から終局まで。あそびかた表示中だけ停止。
- 戦闘演出を通常／簡易の2種に作り直し（溜め・衝突・一拍・転倒・反動／短い接触・軽い揺れ・転倒）。文字・光輪・爆発なし。
- 効果音を追加（外部素材なし、Tools/AudioGenで再現可能に生成した木の乾いた音：選択・着手・衝突・転倒・終局）。視覚イベントと同期（転倒音は着地に一致）。
- 盤＋左右の損失置き場（31枚分を常時確保）を1つの視覚グループとしてプレイ領域の中央へ配置。損失置き場は86%・見出しとの余白を拡大し、自軍・敵軍でTransform・影・卓面距離を共通化。研究モードは思考モニターを閉じている間だけ表示。
- 1.1.1のUI異常（研究モード初期配置で暗転しボタンが効かない）の原因を特定・修正：思考モニターで過去TURNを表示したまま新規対局すると `DrawBeliefs` が毎フレームNullReferenceException。加えてIMGUIの例外ガード（入力状態の復旧・ui-errors.log）とあそびかた／一時停止のウォッチドッグを追加。
- テスト：.NETにプリセット2スイート（全29スイート・2,270,830 checks）。EXE自動検証にプリセット・再起動後の保持・UI状態遷移ストレステスト（各160操作×2）・1.1.1事象の再現・経過時間・効果音同期（通常／簡易×3速度）・音量・損失置き場の共通Transform/見出し余白/中央配置・ツールチップ設定と研究真値を追加。

## 1.1.1 — 2026-09-23 18:40:43 +09:00

- 受入確認のUI/UXをパッチ修正。CPU・評価関数・強さ・戦い方・初期配置ロジック・ルールは変更なし。
- 敵駒ツールチップを両モード共通化。カーソル追従から駒Bounds基準へ変更し、大きな余白・右/左/上下回避・画面端と主要UI回避・細い対象枠を追加。
- PlayerKnownFactsをCPU Knowledgeとは別に実装。公開移動・戦闘から候補が1種類なら「判明：駒名」。撃破後も保持し、敵損失表示は共通裏面のまま。
- 盤上の勝ち／負け／相打ち文字を廃止。終局モーダルは維持。
- カメラ角度とパースを維持して盤面を上方へ配置。損失表示は左右とも3列×最大11段・78%サイズ。
- ゲーム内生成ロゴを2行へトリミング（UserProvided原本を保持）。初期配置画面をCPU設定・自軍の配置・対局開始に区分。
- 全既存テストと追加テスト（2,270,801 checks）成功。720p/900p/1440p/4Kの両モードで全66ノードのクリック・最大31枚の収まりを実EXE検証。
- 1.1.0と決定的CPU評価指標が一致。実EXEの通常実行／毎手モード切替とも39手・同一fingerprint。詳細はTests/ACCEPTANCE-1.1.1.md。

## 1.1.0 — 2026-09-23 17:53:37 +09:00

- 表示モードを「対戦」と「研究」に分離（起動時は対戦）。モードは表示層だけの状態で、対局状態は `GameSession` に分離。対局中も相互に切替可能で、切替は盤面・手番・CPU Knowledge・Seed・乱数・履歴に影響しない。
- 対戦モード：ユーザー提供のタイトルロゴ、TURN・手番、あそびかた・設定・新規対局、盤の左右に自軍／敵軍の損失（3D駒の平置き。自軍は表面・駒種順、敵軍は共通の裏面・撃破順）、下部に状態表示。研究用の情報は表示しない。
- 対局前のCPU設定：強さ（弱・中・強）と戦い方（かなり攻撃的〜かなり防御的）。戦い方は評価の重みと配置思想の選ばれやすさの両方に反映。中・バランスは1.0.0と同じ判断。Seedは内部で保持し対戦モードでは表示しない。
- 「あそびかた」：勝利・敗北・引き分け（本作の対局終了ルール）、16種の駒一覧、駒ごとの戦闘相性（駒画像）・階級の位置・移動図（実際の移動生成）。表示中は対局を一時停止。研究モードからも利用可。
- 研究モード：1.0.0のUIを維持し、強さ・戦い方の選択と表示、あそびかた・対戦モードへの切替を追加。
- アプリアイコンをユーザー提供画像から自動生成してWindowsビルドへ設定。
- 視認性：駒の文字・星・アイコンを濃くし（インクの濃化と太らせ、明るい縁取りを暗い溝影に変更）、駒面の光沢を下げた。木地と裏面の紋章は従来どおり。
- テスト追加：中・バランスと1.0.0の一致、弱は2手読みなし、強さ・戦い方でKnowledge不変、戦い方の配置・評価への反映、弱/強でも敵駒真値を参照しない、あそびかたの相性＝審判・移動＝移動生成。EXE自動検証：起動モード、モード往復と毎手切替での指紋一致、あそびかた中の停止、損失表示、ロゴ、アイコン。

## 1.0.0 — 2026-09-23 16:16:05 +09:00

- 初回作成。Unity 6.6（Windows x64）の31枚型軍人将棋（人間 vs CPU）。
- 盤（各陣8×4、X字突入口2か所と中央丸、総司令部2マス）をグラフ構造で実装し、マス線・X字経路・中央丸・司令部はUnity側で幾何的に構築。固定ルールはREADME.md「採用ルール」。
- 駒・盤テクスチャをユーザー提供の無地駒・アイコン・木目とKSO闘龍フォントから機械的に生成（Tools/TextureGen）。裏面は明るい木目に小さな焼印風の円＋星の紋章。
- 情報隠蔽：真の駒種はEngineのinternalな状態にだけ持ち、CPUアセンブリはRulesとObservationだけを参照。敵駒の表示は裏面のみ、戦闘演出は全駒共通。
- CPU：観測（移動の可否・飛び越し・帯の横断・戦闘結果・初期位置）による候補除外と枚数制約付きの確率推定、信念に基づく期待評価と2手読み。配置思想5種（Formation Seedから自動／明示指定）。Seedは Player Formation / CPU Formation / CPU Decision に分離。
- 思考モニター（採用手・理由・候補手と評価内訳・想定応手・敵駒ごとの推定確率と推定理由）、戦闘履歴、敵駒ホバー情報、演出ON/OFFと速度。
- 検証：.NETテスト（ルール・合法手・全組み合わせの勝敗・司令部占領・Seed再現性・知識更新・情報境界・全局完走）とEXEでの自動対局検証。
