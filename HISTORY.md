# AF-MilitaryShogi 変更履歴

日時は日本標準時（`YYYY-MM-DD HH:MM:SS +09:00`）で記録する。新しい版を上に追加する。

## 1.5.1 — 2026-09-24 17:58:25 +09:00（受入試験中・未確定）

- Android縦画面の初期配置で、「初期配置」タイトルの直下に操作説明「自軍の駒をタップ → 置きたいマスをタップで入れ替え」を常時表示（横画面は下部の状態表示のまま変更なし）。「盤を広く表示」で設定・配置パネルを閉じても消えない。上部バーの高さは変えず、ロゴとタイトルを詰めて空いた部分に置くため、盤の表示領域は変わらない。
- 検証：Pixel 7 Proの実機試験62項目PASS（縦画面の説明表示・パネルを閉じても表示・盤の表示領域不変の3項目を追加）。PC版の動作は変更なし。

## 1.5.0 — 2026-09-24 17:28:43 +09:00（受入試験中・未確定）

- 終局後の **棋譜再現** と **敵駒開示** を追加（PC・Android共通、互換性を保つ機能追加のためマイナー更新）。終局すると最終TURNを表示し、「◀◀ ◀ TURN n / N ▶ ▶▶」で1手（移動・戦闘・結果まで）ずつ戻る・進む。TURN 0＝初期配置。先頭では◀◀◀、末尾では▶▶▶が無効。TURNごとに駒位置・自軍の損失・敵軍の損失（撃破順）・そのTURN時点の判明情報・TURN表示が同期し、未来の情報を過去TURNへ出さない。再現中の駒タップ／ホバーはそのTURN時点の観測情報だけを表示し、駒は動かせない。閲覧専用（再開・分岐・保存・ファイル入出力・自動再生・戦闘ログ表示なし）。
- 「敵駒開示」：情報隠蔽のユーザー指示による例外。対局中は一切開示しない。終局直後も裏向き。ONの時だけ盤上の敵駒と敵軍の損失（並びは撃破順のまま）の正体を表示し、TURN 0ではCPUの初期配置が分かる。毎局OFFで始まり、保存せず、次の対局へ引き継がない。
- 実装：局面は確定したPlayerView（初期位置＋公開の着手記録）から `Observation.GameReplay` で再構成（新しい棋譜データを持たない）、判明情報はそのTURNまでの記録で `PlayerKnownFacts` を作り直す。操作ボタンは `ReplayBar`（矢印はフォントに依存しない三角形）。PC：上部バーのTURN表示の位置（←→・Home・Endキーも可）。Android：縦は上部バーに1段追加、横はTURN表示の位置（右のボタンを少し狭める）。下部のあそびかた・設定・新規対局は変更なし。CPU（強さ・2手読み・評価・重み・配置思想・Knowledge・Decision Seed）は変更なし。
- 検証：.NET 37スイート（新規「全TURNが対局中のビュー・判明情報と一致」15,923 checks）失敗0。Windows実EXE自動検証A/B PASS（指紋72C15D3F2B9D83FF不変、全39TURNの位置・損失・判明情報を対局中の記録と照合、TURN 0開示＝CPU初期配置、損失の順序不変、開示が次の対局へ残らない）。Windows実クリック検証52項目PASS（再現ボタンの有効／無効、開示ON/OFF、再現中に駒を選べない）。Pixel 7 Proの実機試験59項目PASS（再現・開示の14項目を追加。縦横の実タップで再現・開示、TURN 0の観測情報は「正体不明」、新規対局で開示OFF）。

## 1.4.1 — 2026-09-24 11:29:37 +09:00（受入試験中・未確定）

- スマホ実戦のタップミス対策として、自駒を選択中の盤面タップを変更（PC・Android共通）：合法な移動先の空きマス→移動、合法な攻撃先の敵駒→戦闘（従来どおり）、移動先でない敵駒→選択解除して観測情報（Android、従来どおり）、別の自軍駒→選択を切替、選択中の駒自身→選択を維持、その他の盤内マスや盤上のマス以外の場所→何もしない（選択を維持）、盤外→選択解除。近くの移動先への自動補正はしない（誤った移動を推測で実行しない）。
- 検証：Windows実EXE自動検証A/B PASS（指紋72C15D3F2B9D83FF不変、クリック処理経由の選択規則チェック追加）、Windows実クリック検証PASS（移動先でないマス・選択中の駒で選択維持、盤外で解除）、Pixel 7 Proの実機試験45項目PASS（実タップの選択規則4項目を追加、既存操作を含む）。膠着の審判介入（1.4.0）は意図的な膠着の再現が難しいため経過観察。
## 1.4.0 — 2026-09-24 10:50:22 +09:00（受入試験中・未確定）

- 膠着の審判介入を追加。同じ公開局面（全駒の番号と位置・手番）が5回目に現れたら、その反復について一度だけ審判が介入する。介入の相手は、反復に使われている相手の駒に総司令部が近い側（盤上の距離のみ、同距離なら両者）。判定は Observation 層の `RepetitionReferee` で行い、駒の正体・占領可否・CPUの推定や評価値・完全情報の勝敗を使わない。
- プレイヤーへの介入：「同じ局面が繰り返されています／投了するか続行するか」を一度だけ表示（Modal、対局と時計を停止）。続行後の手に制限なし。勝敗・駒の正体を示唆しない。
- CPUへの介入：以後、CPUの選んだ手がその反復に戻る場合だけ、CPU自身の評価で次に良い合法手に替える（`RefereeChoice`。評価関数・既存の反復抑制は変更なし）。短い審判メッセージを表示。
- 検証：.NET 36スイート2,530,813 checks失敗0、新規5スイート（反復なしで介入しない・5回目で一度だけ介入・敵駒の正体を入れ替えても同じ介入・脅かされている側への介入・CPUの次善手と評価不変）。CPU指標は1.1.1と全行一致。Windows実EXE自動検証A/B PASS（指紋72C15D3F2B9D83FFで不変）。Pixel 7 Proで膠着の審判確認のModal・一時停止・時計停止・続行を実タップで確認。
## 1.3.0 — 2026-09-23 23:59:10 +09:00

- 1.2.2を基準としたリリース候補。新しい終局条件・投了・Android版（対戦モードのみ）を追加したため、互換性を保つ機能追加としてマイナー更新。1.2.1・1.2.2の受入修正（効果音の出力、ModalInput）を含む。CPU（強さ・2手読み・評価・重み・配置思想・推定・Knowledge・Decision Seed・情報隠蔽）は変更なし。
- 終局：双方とも占領可能駒（大将〜少佐）が0になったら引き分け（EndReason.NoCapturers）。プレイヤーだけ0になった瞬間、審判が一局に一度だけ投了を確認（続行／投了）。投了はCPUの勝ち（EndReason.Resigned）。CPUは投了せず、CPU側だけ0になっても通知しない。README・あそびかたに反映。
- 審判の確認と終局結果を `ModalInput` のModalに登録（背後入力遮断、確認中は対局・時計停止）。終局結果に「盤面を見る」。
- 盤と左右の損失置き場の間に損失駒の横幅の半分の空き（左右対称、駒サイズから算出。86%を維持）。設定の効果音行と音量行の間に余白。
- Android版：対戦モード・あそびかた・設定・プリセット・CPU設定・損失表示・観測情報・経過時間・戦闘演出・効果音。研究モードは搭載しない。縦横両対応（回転は表示だけを組み直し、対局状態は不変）、Safe Area、dp単位の大きなタップ領域、自駒タップ→移動先タップ、敵駒タップで観測情報、バックグラウンドで一時停止・同じ対局へ復帰、Androidのソフトウェアキーボードでプリセット名入力、戻るキーで最前面のModalを閉じる。ゲーム本体はPC版と共通コード、Android専用は `MobileUi` のみ。
- Android Release APK（ARM64/IL2CPP、専用ローカル鍵で署名、一般配布しない）：`Build-Android.ps1`・`Verify-Android.ps1`。実機試験 `Test-Android.ps1`（実タップ・回転・ホーム・Modal・プリセット・効果音、`-remote` 試験ハーネス）。
- Windows配布ZIP：`Package-Windows.ps1`（100MB未満・同梱README）→ `Prepare-Release.ps1` で `Distribution/`。
- 検証：.NET 31スイート2,521,180 checks失敗0、CPU指標は1.1.1と全行一致（新ルール・旧ルールとも）。Windows実EXE自動検証A/B PASS（指紋72C15D3F2B9D83FF＝1.1.1）、実クリックModal検証37項目・実出力SE検証PASS、配布ZIP 43.6MB。Android Release APK（21.3MB）をGalaxy S26 Ultraで実タッチ試験41項目PASS（回転中の対局もPCと同じ指紋）。詳細は Tests/ACCEPTANCE-1.3.0.md。最終的なタップ感・縦横レイアウト・SE聴感・実機ライフサイクルは人間の受入で確認。

## 1.2.2 — 2026-09-23 22:23:12 +09:00（受入試験中・未確定）

- 設定・あそびかた表示中に背後のUI・盤面へクリックが届く不具合を修正（人間受入試験で確認：設定の「閉じる」が真下の「おまかせ配置」も押す、あそびかた中に背後の「研究モードへ」で遷移する、盤面操作が通る）。原因：IMGUIはコントロール間・コンポーネント間で遮蔽がなく、盤面はパネル矩形（UiRects）だけを避けていたため。
- 共通のModal入力制御 `ModalInput` を追加。設定・あそびかた・新規対局の確認をModalとして登録し、最前面のModalだけがポインター入力を持つ。背後のIMGUIにはポインター/キーイベントを渡さず（ホバー・押下表示もなし）、盤面のホバー・選択・移動も止める。Modal外クリックは閉じずに消費。Modalが閉じた後はマウスボタンを離すまで背後入力を止め、閉じたクリックが背後へ届かない。Modal表示中は背後向けのショートカット（F1・F2・研究モードのM/H/F/N）も無効、Escは最前面のModalだけを閉じる。
- Modalと一時停止は別。設定：入力のみModal、時計は進む。あそびかた：Modal＋一時停止、時計停止（1.2.xの仕様を維持）。
- 実クリック検証 `-modalprobe FILE` を追加（Windowsのカーソル移動・クリックでOS経由の入力を再現）。Modal無効化ビルドで不具合を再現できることを確認したうえで、修正版で全項目PASS（2回）。フル回帰試験は未実施（リリース候補段階で実施）。
## 1.2.1 — 2026-09-23 21:40:14 +09:00（受入試験中・未確定）

- 1.2.0の人間受入試験で判明した「効果音が一切出ない」不具合を修正。原因：カメラを実行時に `AddComponent<Camera>()` で生成しており、AudioListenerが1つも存在しなかったため、Unityが何もミックス出力していなかった（直接の `PlayOneShot` でも出力ピーク0）。メインカメラにAudioListenerを追加。
- 効果音クリップを起動時にデコード（初回の再生が遅れない・落ちないように）。
- 実出力の診断 `-audioprobe FILE` を追加：AudioListener・AudioSource・AudioClipの実行時状態と、`AudioListener.GetOutputData` による出力ミックスのピークを、直接再生・駒クリック経由・効果音OFF・音量100%/50%で測定。1.2.0のタイミング検証は再生要求の時刻しか見ておらず、可聴出力を保証していなかった。EXE自動検証にもAudioListenerの存在確認を追加。
- 検証は軽量のみ（ビルド、修正前後の `-audioprobe`）。フル回帰試験は未実施で、リリース候補の段階でまとめて実施する。最終判定は人の実聴。
## 1.2.0 — 2026-09-23 21:00:02 +09:00

- 1.1.1を基準点とした機能追加。CPU（強さ・2手読み・評価・重み・配置思想・Knowledge・Decision Seed・情報隠蔽）とルールは変更なし。決定的CPU評価指標は1.1.1と全行一致、実EXEの指紋も1.1.1と同一（72C15D3F2B9D83FF）。
- 配置プリセット5枠（両モード）：初期名「プリセット1〜5」、保存時は枠の現在名を初期値にした名前欄のみ。各マスのPieceTypeを形式バージョン付きで保存し、保存・読込時に検証して不正データを拒否。CPUのSeed・配置・Knowledgeは不変。再起動後も保持。
- 研究モード初期配置の重複ボタン「おまかせ配置（Player Seedで再配置）」を削除（Seedの適用／乱数に一本化）。
- 1クリックのモード切替（「研究モードへ」／「対戦モードへ」、F2）。設定画面からモード切替を削除。
- 設定画面：戦闘演出（通常／簡易）・速度（×1／×2／×4）・敵駒の観測情報（ON／OFF）・効果音（ON／OFF）・音量。即時反映・保存。
- 研究モード「CPU駒の正体を表示」ONで盤上のCPU駒の表面・ツールチップにも真値を表示（OFFで判明／正体不明へ戻る）。対戦モードでは常に無効、損失表示は裏面のまま。
- 経過時間（ヘッダー・結果画面）：対局開始から終局まで。あそびかた表示中だけ停止。
- 戦闘演出を通常／簡易の2種に作り直し（溜め・衝突・一拍・転倒・反動／短い接触・軽い揺れ・転倒）。文字・光輪・爆発なし。
- 効果音を追加（外部素材なし、Tools/AudioGenで再現可能に生成した木の乾いた音：選択・着手・衝突・転倒・終局）。視覚イベントと同期（転倒音は着地に一致）。
- 盤＋左右の損失置き場（31枚分を常時確保）を1つの視覚グループとしてプレイ領域の中央へ配置。損失置き場は86%・見出しとの余白を拡大し、自軍・敵軍でTransform・影・卓面距離を共通化。研究モードは思考モニターを閉じている間だけ表示。
- 1.1.1のUI異常（研究モード初期配置で暗転しボタンが効かない）の原因を特定・修正：思考モニターで過去TURNを表示したまま新規対局すると `DrawBeliefs` が毎フレームNullReferenceException。加えてIMGUIの例外ガード（入力状態の復旧・ui-errors.log）とあそびかた／一時停止のウォッチドッグを追加。
- テスト：.NETにプリセット2スイート（全29スイート・2,270,830 checks）。EXE自動検証にプリセット・再起動後の保持・UI状態遷移ストレステスト（各160操作×2）・1.1.1事象の再現・経過時間・効果音同期（通常／簡易×3速度）・音量・損失置き場の共通Transform/見出し余白/中央配置・ツールチップ設定と研究真値を追加。

## 1.1.1 — 2026-09-23 18:40:43 +09:00

- 受入確認のUI/UXをパッチ修正。CPU・評価関数・強さ・戦い方・初期配置ロジック・ルールは変更なし。
- 敵駒ツールチップを両モード共通化。カーソル追従から駒Bounds基準へ変更し、大きな余白・右/左/上下回避・画面端と主要UI回避・細い対象枠を追加。
- PlayerKnownFactsをCPU Knowledgeとは別に実装。公開移動・戦闘から候補が1種類なら「判明：駒名」。撃破後も保持し、敵損失表示は共通裏面のまま。
- 盤上の勝ち／負け／相打ち文字を廃止。終局モーダルは維持。
- カメラ角度とパースを維持して盤面を上方へ配置。損失表示は左右とも3列×最大11段・78%サイズ。
- ゲーム内生成ロゴを2行へトリミング（UserProvided原本を保持）。初期配置画面をCPU設定・自軍の配置・対局開始に区分。
- 全既存テストと追加テスト（2,270,801 checks）成功。720p/900p/1440p/4Kの両モードで全66ノードのクリック・最大31枚の収まりを実EXE検証。
- 1.1.0と決定的CPU評価指標が一致。実EXEの通常実行／毎手モード切替とも39手・同一fingerprint。詳細はTests/ACCEPTANCE-1.1.1.md。

## 1.1.0 — 2026-09-23 17:53:37 +09:00

- 表示モードを「対戦」と「研究」に分離（起動時は対戦）。モードは表示層だけの状態で、対局状態は `GameSession` に分離。対局中も相互に切替可能で、切替は盤面・手番・CPU Knowledge・Seed・乱数・履歴に影響しない。
- 対戦モード：ユーザー提供のタイトルロゴ、TURN・手番、あそびかた・設定・新規対局、盤の左右に自軍／敵軍の損失（3D駒の平置き。自軍は表面・駒種順、敵軍は共通の裏面・撃破順）、下部に状態表示。研究用の情報は表示しない。
- 対局前のCPU設定：強さ（弱・中・強）と戦い方（かなり攻撃的〜かなり防御的）。戦い方は評価の重みと配置思想の選ばれやすさの両方に反映。中・バランスは1.0.0と同じ判断。Seedは内部で保持し対戦モードでは表示しない。
- 「あそびかた」：勝利・敗北・引き分け（本作の対局終了ルール）、16種の駒一覧、駒ごとの戦闘相性（駒画像）・階級の位置・移動図（実際の移動生成）。表示中は対局を一時停止。研究モードからも利用可。
- 研究モード：1.0.0のUIを維持し、強さ・戦い方の選択と表示、あそびかた・対戦モードへの切替を追加。
- アプリアイコンをユーザー提供画像から自動生成してWindowsビルドへ設定。
- 視認性：駒の文字・星・アイコンを濃くし（インクの濃化と太らせ、明るい縁取りを暗い溝影に変更）、駒面の光沢を下げた。木地と裏面の紋章は従来どおり。
- テスト追加：中・バランスと1.0.0の一致、弱は2手読みなし、強さ・戦い方でKnowledge不変、戦い方の配置・評価への反映、弱/強でも敵駒真値を参照しない、あそびかたの相性＝審判・移動＝移動生成。EXE自動検証：起動モード、モード往復と毎手切替での指紋一致、あそびかた中の停止、損失表示、ロゴ、アイコン。

## 1.0.0 — 2026-09-23 16:16:05 +09:00

- 初回作成。Unity 6.6（Windows x64）の31枚型軍人将棋（人間 vs CPU）。
- 盤（各陣8×4、X字突入口2か所と中央丸、総司令部2マス）をグラフ構造で実装し、マス線・X字経路・中央丸・司令部はUnity側で幾何的に構築。固定ルールはREADME.md「採用ルール」。
- 駒・盤テクスチャをユーザー提供の無地駒・アイコン・木目とKSO闘龍フォントから機械的に生成（Tools/TextureGen）。裏面は明るい木目に小さな焼印風の円＋星の紋章。
- 情報隠蔽：真の駒種はEngineのinternalな状態にだけ持ち、CPUアセンブリはRulesとObservationだけを参照。敵駒の表示は裏面のみ、戦闘演出は全駒共通。
- CPU：観測（移動の可否・飛び越し・帯の横断・戦闘結果・初期位置）による候補除外と枚数制約付きの確率推定、信念に基づく期待評価と2手読み。配置思想5種（Formation Seedから自動／明示指定）。Seedは Player Formation / CPU Formation / CPU Decision に分離。
- 思考モニター（採用手・理由・候補手と評価内訳・想定応手・敵駒ごとの推定確率と推定理由）、戦闘履歴、敵駒ホバー情報、演出ON/OFFと速度。
- 検証：.NETテスト（ルール・合法手・全組み合わせの勝敗・司令部占領・Seed再現性・知識更新・情報境界・全局完走）とEXEでの自動対局検証。
