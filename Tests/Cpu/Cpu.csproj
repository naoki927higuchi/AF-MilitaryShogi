<Project Sdk="Microsoft.NET.Sdk">
  <!-- Compiles Assets/Scripts/Cpu exactly like the Unity asmdef of the same name.
       References mirror the asmdef so an illegal dependency fails to build here too. -->
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <LangVersion>9.0</LangVersion>
    <Nullable>disable</Nullable>
    <AssemblyName>MilitaryShogi.Cpu</AssemblyName>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="../../Assets/Scripts/Cpu/**/*.cs" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="../Rules/Rules.csproj" />
    <ProjectReference Include="../Observation/Observation.csproj" />
  </ItemGroup>
</Project>
